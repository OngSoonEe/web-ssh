{
  "name": "web-ssh",
  "version": "1.0.0",
  "description": "Single-app browser SSH terminal: log in from any browser and SSH into any host the server can reach (e.g. Tailscale machines).",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node test/e2e-test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "express": "^5.2.1",
    "ssh2": "^1.17.0",
    "ws": "^8.21.3"
  }
}
